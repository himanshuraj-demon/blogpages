<!DOCTYPE html>
<html lang="en">
<head>
    <%- include("./partials/head") %>
    <title>Page Not Found</title>

<style>

body {
    background: linear-gradient(135deg, #0f172a, #020617);
    height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    color: white;
    overflow: hidden;
}

/* Card */
.error-card {
    text-align: center;
    padding: 50px;
    border-radius: 15px;
    backdrop-filter: blur(15px);
    background: rgba(255,255,255,0.05);
    box-shadow: 0px 0px 30px rgba(0,0,0,0.5);
    max-width: 500px;
    width: 90%;
}

/* Animated 404 */
.error-code {
    font-size: 100px;
    font-weight: bold;
    color: #38bdf8;
    animation: float 3s ease-in-out infinite, glow 2s ease-in-out infinite alternate;
}

/* Floating animation */
@keyframes float {
    0% { transform: translateY(0px); }
    50% { transform: translateY(-10px); }
    100% { transform: translateY(0px); }
}

/* Glow animation */
@keyframes glow {
    from {
        text-shadow: 0 0 10px #38bdf8;
    }
    to {
        text-shadow: 0 0 25px #38bdf8, 0 0 40px #38bdf8;
    }
}

.error-text {
    opacity: 0.8;
    margin-bottom: 25px;
}

.home-btn {
    border-radius: 30px;
    padding: 10px 25px;
    transition: 0.3s;
}

.home-btn:hover {
    transform: scale(1.05);
}

</style>
</head>

<body>

<div class="error-card">

    <div class="error-code">404</div>

    <h3 class="mb-3">Oops! Page Not Found</h3>

    <p class="error-text">
        The page you are looking for doesn't exist or something went wrong.
    </p>

    <a href="/" class="btn btn-info home-btn">
        Go Back Home
    </a>

</div>
<script>
setTimeout(()=>{
    window.location.href = "/";
}, 6000);
</script>

</body>
</html>
